/target
.claude/plan-bridge-state.json
.claude/plan-bridge-state.json.lock
.claude/plan-bridge-debug.jsonl

# the real board list is private by nature; only config.example.toml ships
/config.toml

# build.rs writes an absolute DATABASE_URL here
.env
